import { SafeAreaView } from "react-native";
import Feed from "../../Components/Feed";

const Home = () => {
  return (
    <SafeAreaView>
      <Feed/>
    </SafeAreaView>
  );
};

export default Home;
